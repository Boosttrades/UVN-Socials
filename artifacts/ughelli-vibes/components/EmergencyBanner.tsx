// EmergencyBanner renders a pinned alert strip when there is an active emergency post.
// Currently hidden — no posts in the feed yet. Will be re-enabled once real posts exist.
export default function EmergencyBanner() {
  return null;
}
